package pro.midev.mec.presentation.ui.screens.main_profile

import pro.midev.mec.presentation.base.BaseViewModel

class MainProfileViewModel(
    // todo
) : BaseViewModel<MainProfileState, MainProfileEvent, MainProfileAction>(MainProfileState()) {
    override fun obtainEvent(event: MainProfileEvent) {
        when (event) {
            else -> {}
        }
    }
}